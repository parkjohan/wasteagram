import 'package:flutter/material.dart';

class AddWasteView extends StatelessWidget {
  const AddWasteView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: CircularProgressIndicator(),
    );
  }
}